# == Class: <%= metadata.name %>
#
# === <%= metadata.name %> documentation
#
class <%= metadata.name %> inherits <%= metadata.name %>::params{

  class { '::<%= metadata.name %>::install': } ->
  class { '::<%= metadata.name %>::config': } ~>
  class { '::<%= metadata.name %>::service': } ->
  Class['::<%= metadata.name %>']

}
